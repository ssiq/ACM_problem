#include <iostream>

#include <limits>

const int MAX_INT=std::numeric_limits<int>::max();
const int MIN_INT=std::numeric_limits<int>::min();
const int MAX_LL=std::numeric_limits<long long int>::max();
const int MIN_LL=std::numeric_limits<long long int>::min();

#define DEBUG_OUT(a) cout<<a<<endl
// #define DEBUG_OUT(a)

#define DISPLAY_VECTOR(v){for(int i=0;i<v.size();++i)cout<<v[i]<<" ";cout<<endl;}
typedef long long int LL;

using namespace std;

void solve()
{

}

int main()
{
	int T;
	cin>>T;
	for(int i=0;i<T;++i)
	{
		solve();
	}
}